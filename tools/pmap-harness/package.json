{
  "name": "pmap-prompt-harness",
  "version": "0.1.0",
  "description": "Autonomous prompt-refinement harness for PlanetMap AI system. Drives gamma-node-editor.html in headless Chrome via Puppeteer, iterates the system prompt against reference heightmaps until output stylistically matches.",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "node setup.mjs",
    "render": "node render.mjs",
    "smoke": "node smoke-test.mjs"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
